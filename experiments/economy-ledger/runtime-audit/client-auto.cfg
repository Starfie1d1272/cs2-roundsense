// client-auto.cfg — MANUAL protocol support file (NOT automation).
// There are NO cell_*.cfg automation files: the matrix is executed by hand
// in the console using the steps below. record the whole session:
//   record buytest
//
// MATRIX CELL TEMPLATE (manual, per cell):
//   mp_restartgame 1                      → full reset (money + counters)
//   lose N rounds to reach the target state (do nothing; die)
//   [if GSI/netvar readable] verify actual consecutive_round_losses equals
//       the target state BEFORE executing the win — if the field is NOT
//       readable, the state is a modeled precondition, not direct.
//   execute the target win EXACTLY once:
//     time_ran_out : as T never plant; timer expires (CT wins)
//     bomb_defused : T plants, CT defuses
//     target_bombed: T plants, bomb explodes (T wins)
//     elimination  : kill all 5 opponents (explicit side chosen by you)
//   lose the next round once (do nothing)
//   dump convars (below), record GSI payloads (recorder running)
//
// Bots / sides (keep 5v5 — no shorthanded / takeover / auto-balance):
//   bot_quota_mode fill
//   bot_add_ct ; bot_add_t
//   bot_stop 1                            (freeze bots while you set up)
//   jointeam spectate ; jointeam ct ; jointeam t
//   mp_autoteambalance 0 ; mp_limitteams 0
//
// Per-cell convar dump (before AND after the cell):
//   find mp_starting_losses
//   find mp_consecutive_loss
//   find cash_team_loser_bonus
//   find mp_buytime
//
// GSI: gamestate_integration_roundsense_lossbonus.cfg copied to cfg dir
// BEFORE game start; verify `find gamestate_integration` — the console load
// command is NOT assumed to exist on build 24537688.
//
// Capture convar help once:
//   help mp_buytime
//   help mp_starting_losses
